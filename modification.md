# 📋 [카카오톡 회의 기반] 보행 분석 알고리즘 및 진단 솔루션 구현 명세서

본 문서는 팀 내 회의(카카오톡 대화)를 통해 결정된 사항인 **'기존 데이터 대비 상대적 비교 방식의 한계 극복'** 및 **'특정 부위 압력 기반의 규칙(if문) 진단 및 솔루션 도출'**을 백엔드(FastAPI)와 프론트엔드 대시보드에 실제로 구현하기 위한 상세 개발 명세서입니다.

---

## 1. 핵심 방향성 및 타협점

* **기존 방식의 문제점 발견:** 사용자 본인의 평소 걸음걸이를 정상 지표로 삼을 경우, 이미 과내전 등 보행 질환이 있는 사용자는 '오염된 데이터'가 정상 지표로 등록되는 치명적인 오류가 발생함.
* **최종 변경 및 확정안:** 복잡한 인공지능(AI) API 연동이나 사용자 기준 데이터 수집을 배제하고, **의학적/신체적 이론에 기반한 특정 센서의 압력 불균형 상태를 `if`문(Rule-based)으로 감지**하여 위험 점수와 매칭되는 솔루션 텍스트를 실시간으로 출력함.

---

## 2. 백엔드(FastAPI) 실시간 진단 알고리즘 로직

센서 데이터 수신 루프(`POST /update`) 내에서 각 채널의 `_kpa` 수치를 비교 연산하여 실시간 보행 상태를 판정합니다.

### ① 과내전 위험 (Overpronation) 감지 알고리즘
* **판정 기준:** 발이 지면에 닿아 있는 상태(뒤꿈치 혹은 앞꿈치 압력 존재)에서, 발 안쪽 압력(`ball_in`)이 바깥쪽 압력(`ball_out`)에 비해 지속적으로 현저히 높을 때 유도합니다.
* **공식 및 예시 코드:**
  ```python
  # 왼발 과내전 체크 예시 (오른발도 동일 로직 적용)
  left_in = data.left.sensors.s2_kpa
  left_out = data.left.sensors.s3_kpa

  if left_in > (left_out * 1.5) and left_in > 30.0:
      status = "과내전 위험 감지"
      issue_zone = "안쪽 아치 중심 (CH1)"
      solution = "발목이 안쪽으로 무너지고 있습니다. 기능성 아치 패드 사용 및 의도적으로 발가락 바깥쪽에 힘을 주는 보행을 권장합니다."